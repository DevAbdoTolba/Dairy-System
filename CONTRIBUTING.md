# Contributing

Use Node 24 and npm. Keep changes modular and small, add tests with changed business behavior, run `npm run verify`, and use Conventional Commit messages. Do not add data files, secrets, animations, or unnecessary dependencies.
